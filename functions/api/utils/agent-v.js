/**
 * Agent V：监督校验
 * 输入：所有Agent输出 + 原始输入数据
 * 输出：监督校验报告
 */

export function getVPrompt(allOutputs, originalInput) {
  const { a1, a2, b, c, d } = allOutputs;
  const { scores, manualReport, targetRole, learningStyle, timeResource, specialNotes } = originalInput;

  return `# 任务：全局监督校验

## 输入数据
### 原始输入
- 六维评分：${JSON.stringify(scores)}
- 人工报告：${manualReport ? '有' : '无'}
- 目标职位：${targetRole || '无'}
- 学习风格：${learningStyle || '未指定'}
- 时间资源：${timeResource || '未指定'}
- 特殊注意：${specialNotes || '无'}

### Agent输出（摘要）
- A1诊断：${a1.substring(0, 200)}...
- A2归因：${a2.substring(0, 200)}...
- B匹配：${b.substring(0, 150)}...
- C设计：${c.substring(0, 150)}...
- D验证：${d.substring(0, 150)}...

## 校验项目
你必须对以下9个项目逐一检查，给出结论（通过/警告/失败）和理由。

### 检查1：输入数据完整性
- **标准**：六维评分齐全（6个维度），每个分值在0-10之间。
- **检查方法**：验证原始输入。
- **输出格式**：通过/警告/失败 + 理由。

### 检查2：病毒-归因匹配度
- **标准**：B选择的病毒变体必须与A2归因的关键词匹配。
- **强制对应表**（必须遵守）：
  - 归因关键词“从不质疑” → 深度追问型
  - 归因关键词“拒绝使用” → 工具效率型
  - 归因关键词“一团乱麻” → 分解触发型
  - 归因关键词“缺乏目标” → 量化思维型
  - 归因关键词“害怕修改” → 迭代优先型
  - 归因关键词“没有边界” → 约束创新型
  - 归因关键词“依赖捷径” → 反常识型
  - 归因关键词“责任扩散” → 责任界定型
- **检查方法**：在A2输出中搜索关键词，核对B的病毒变体。
- **输出格式**：通过/警告/失败 + 匹配到的关键词或未匹配说明。

### 检查3：课程–病毒一致性
- **标准**：C课程设计中必须包含B选择的病毒变体。
- **检查方法**：在C输出中搜索病毒变体名称。
- **输出格式**：通过/警告（未明确提及）/失败（完全未提及）。

### 检查4：验证–课程对应性
- **标准**：D的验证标准必须针对C课程中的植入点。
- **检查方法**：对比C的植入点描述和D的验证标准。
- **输出格式**：通过/警告（部分对应）/失败（完全不相关）。

### 检查5：伪装性检查
- **标准**：C课程表面必须是正经技能培训，不能出现“病毒”“劫持”“植入”等敏感词。
- **检查方法**：在C输出中搜索敏感词列表：[病毒, 劫持, 植入, 感染, 篡改, 操纵]。
- **输出格式**：通过/警告（出现1次）/失败（出现≥2次）。

### 检查6：可行性检查
- **标准**：课程总时长不能超过原始输入中的时间资源限制（如有）。
- **检查方法**：提取C课程总时长，与timeResource对比。
- **输出格式**：通过/警告（略超）/失败（严重超限）。

### 检查7：个性化适配
- **标准**：C课程设计应考虑学习风格偏好（如有）。
- **检查方法**：如果learningStyle有指定，检查C中是否有相应设计（如“视觉型”应有图表）。
- **输出格式**：通过/警告（未明确适配）/失败（完全忽略）。

### 检查8：风险覆盖
- **标准**：D的验证设计中必须有风险应对预案。
- **检查方法**：检查D输出中是否有“风险应对预案”部分。
- **输出格式**：通过/警告（预案不充分）/失败（无预案）。

### 检查9：整体逻辑连贯性
- **标准**：从A1崩塌维度 → A2归因 → B病毒 → C课程 → D验证，逻辑链必须连贯。
- **检查方法**：用一句话描述整个逻辑链，检查是否有断点。
- **输出格式**：通过/警告（有轻微跳跃）/失败（逻辑断裂）。

## 输出格式要求
你必须严格按照以下格式输出，不得添加任何解释性前缀。

### 监督校验报告
**总体结论**：[通过/警告/失败]（基于9个检查的加权评估）

**详细检查结果**：
1. 检查1（输入完整性）：[通过/警告/失败] - [理由]
2. 检查2（病毒-归因匹配）：[通过/警告/失败] - [匹配情况]
3. 检查3（课程-病毒一致）：[通过/警告/失败] - [一致程度]
4. 检查4（验证-课程对应）：[通过/警告/失败] - [对应情况]
5. 检查5（伪装性检查）：[通过/警告/失败] - [敏感词出现情况]
6. 检查6（可行性检查）：[通过/警告/失败] - [时长适配情况]
7. 检查7（个性化适配）：[通过/警告/失败] - [学习风格适配情况]
8. 检查8（风险覆盖）：[通过/警告/失败] - [预案充分性]
9. 检查9（逻辑连贯性）：[通过/警告/失败] - [逻辑链描述]

**关键问题摘要**：
- 必须修复的问题：[列出所有“失败”项目]
- 建议优化的问题：[列出所有“警告”项目]
- 可忽略的小问题：[列出不影响整体的小问题]

**最终建议**：
- 如果总体结论为“通过”：方案可以交付。
- 如果总体结论为“警告”：方案可以交付，但需注意优化点。
- 如果总体结论为“失败”：方案需重新设计，重点修复[关键问题]。

## 注意事项
1. 检查时要严格，但也要合理，允许一定的灵活性。
2. 如果原始输入中某些字段缺失，相应检查可标记为“不适用”。
3. 不要重复Agent们已经说过的话，重点在**逻辑校验**和**一致性验证**。

## 输出示例
（此处省略示例，你只需按格式输出即可）

开始输出：`;
}