/**
 * Agent B：病毒匹配引擎
 * 输入：六维评分 + A1输出 + A2输出 + 目标职位 + 权重版本
 * 输出：匹配结果 + 匹配理由
 */

// 导入权重表
import { WEIGHT_TABLE, getVirusWeight, getForcedVariant, getDefaultVariant } from './weight-table.js';

// 权重表版本号：从权重表导入
export const CURRENT_WEIGHT_VERSION = WEIGHT_TABLE.version;

export function getBPrompt(scores, a1Output, a2Output, targetRole, weightVersion) {
  return `# 任务：认知病毒匹配

## 输入数据
1. 学员六维评分：${JSON.stringify(scores)}
2. A1漏洞诊断摘要：
${a1Output.substring(0, 500)}...
3. A2归因分析摘要：
${a2Output.substring(0, 500)}...
4. 目标职位：${targetRole || '未指定'}
5. 权重表版本：${weightVersion || CURRENT_WEIGHT_VERSION}

## 认知病毒库（六类）
每类病毒针对一个思维维度，每类有多个变体（权重不同）：

### 1. 工具本位病毒
- **工具效率型**（权重${getVirusWeight('工具效率型')}）：针对“拒绝使用工具”的崩塌，通过展示工具压倒性效率来植入
- **工具依赖型**（权重${getVirusWeight('工具依赖型')}）：针对“工具使用不熟练”的预警，通过降低工具使用门槛植入
- **工具创造型**（权重${getVirusWeight('工具创造型')}）：针对“只会用现成工具”的薄弱，引导自主开发小工具

### 2. 本质主义病毒
- **深度追问型**（权重${getVirusWeight('深度追问型')}）：针对“从不质疑底层逻辑”的崩塌，训练连续追问5个为什么
- **模型映射型**（权重${getVirusWeight('模型映射型')}）：针对“容易被表面带偏”的预警，提供标准思维模型作为参照
- **反常识型**（权重${getVirusWeight('反常识型')}）：针对“偶尔会追问但不够”的薄弱，用反常识案例激发质疑

### 3. 模块化病毒
- **分解触发型**（权重${getVirusWeight('分解触发型')}）：针对“问题一团乱麻”的崩塌，强制拆解到最小可执行单元
- **接口标准化型**（权重${getVirusWeight('接口标准化型')}）：针对“模块耦合度高”的预警，定义清晰的输入输出接口
- **模块复用型**（权重${getVirusWeight('模块复用型')}）：针对“拆解能力有限”的薄弱，展示复用带来的效率提升

### 4. 结果导向病毒
- **量化思维型**（权重${getVirusWeight('量化思维型')}）：针对“缺乏目标意识”的崩塌，一切目标必须可量化
- **关键路径型**（权重${getVirusWeight('关键路径型')}）：针对“陷入过程细节”的预警，识别并专注关键路径
- **结果可视化型**（权重${getVirusWeight('结果可视化型')}）：针对“有时忘记目标”的薄弱，用看板等工具可视化进度

### 5. 迭代式病毒
- **迭代优先型**（权重${getVirusWeight('迭代优先型')}）：针对“害怕修改”的崩塌，强调“先有再优”的文化
- **反馈闭环型**（权重${getVirusWeight('反馈闭环型')}）：针对“迭代意识弱”的预警，建立快速反馈机制
- **版本管理型**（权重${getVirusWeight('版本管理型')}）：针对“追求一次完美”的薄弱，引入版本管理思维

### 6. 边界思维病毒
- **约束创新型**（权重${getVirusWeight('约束创新型')}）：针对“完全没有边界概念”的崩塌，在明确约束下激发创新
- **责任界定型**（权重${getVirusWeight('责任界定型')}）：针对“边界意识模糊”的预警，清晰划分权责利
- **系统思维型**（权重${getVirusWeight('系统思维型')}）：针对“容易责任扩散”的薄弱，培养系统视角看边界

## 匹配规则
1. **维度优先级**：只针对A1表中崩塌状态为“崩塌”或“预警”的维度，按崩塌严重程度降序选择。
2. **归因适配**：根据A2归因的关键词，选择最适合的病毒变体：
   - 如果A2归因包含“原生家庭”“童年” → 选择权重较低的变体（如0.4-0.6）
   - 如果A2归因包含“职业经历”“固化” → 选择权重中等的变体（如0.6-0.8）
   - 如果A2归因包含“刻意训练缺失”“思维捷径” → 选择权重较高的变体（如0.8-0.9）
   - 如果A2归因包含“文化环境”“地域” → 考虑目标职位的地域特性
3. **目标职位适配**：如果目标职位明确，选择与该职位核心能力最相关的病毒变体。
4. **强制对应表**（必须遵守）：
   - 归因关键词“从不质疑” → 深度追问型
   - 归因关键词“拒绝使用” → 工具效率型
   - 归因关键词“一团乱麻” → 分解触发型
   - 归因关键词“缺乏目标” → 量化思维型
   - 归因关键词“害怕修改” → 迭代优先型
   - 归因关键词“没有边界” → 约束创新型
   - 归因关键词“依赖捷径” → 反常识型
   - 归因关键词“责任扩散” → 责任界定型

- 如果A2的归因描述不包含上述任何关键词：默认选择该病毒类型下最通用的变体（工具效率型、深度追问型、分解触发型、量化思维型、迭代优先型、约束创新型），并在匹配理由中标注"归因关键词未命中，使用默认变体"。

## 输出格式要求
你必须严格按照以下格式输出，不得添加任何解释性前缀。

### 匹配结果
【病毒类型】：[选择的病毒类型，如“工具本位病毒”]
【病毒变体】：[选择的变体名，如“工具效率型”]
【目标维度】：[针对的维度，如“工具本位”]
【权重值】：[该变体的权重，如0.8]

### 匹配理由
用一段话（150-200字）说明为什么选择这个变体：
1. 引用A1诊断中的崩塌状态
2. 引用A2归因中的关键线索
3. 说明该变体如何针对性地解决归因问题
4. 如果适配了目标职位，说明适配点
5. 如果使用了强制对应表，标注“依据强制对应表”

### 预期植入效果
- 短期（1周内）：[预期可观察的行为改变]
- 中期（1个月内）：[预期思维模式转变]
- 长期（3个月以上）：[预期维度得分提升目标]

## 注意事项
1. 如果多个维度崩塌，优先选择最崩塌（得分最低）且归因最清晰的维度。
2. 如果权重表版本不同，以本提示中的权重为准。
3. 不要创造新的病毒变体，只能从上述库中选择。

## 输出示例
（此处省略示例，你只需按格式输出即可）

开始输出：`;
}